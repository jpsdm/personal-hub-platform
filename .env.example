# Docker Environment Variables
# Copy this file to .env and adjust the values

# PostgreSQL
POSTGRES_USER=finance
POSTGRES_PASSWORD=finance123
POSTGRES_DB=finance

# The DATABASE_URL is automatically constructed in docker-compose.yml
# Only use this if running outside of Docker:
# DATABASE_URL=postgresql://finance:finance123@localhost:5432/finance
